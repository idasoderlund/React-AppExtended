/*DEL 3!!!!!! SITTA IHOP MED DEL 2

Komponent 2 är en sidebar som ska innehålla flera button och de har
samma design med hover effecr. buttons ska vara en komponent
som vi anropar inuti sidemenu komponent.*/

/*import React, { useState } from "react";

function Button({ label, onClick }) {
  return (
    <button onClick={onClick} className="sidebar-button">
      {label}
    </button>
  );
}

export default Button;*/
